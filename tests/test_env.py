import socket
import sqlite3
import pandas as pd
import matplotlib.pyplot as plt

print("🎉 Setup Successful!")
print(f"SQLite version running: {sqlite3.sqlite_version}")
print("Ready to start coding the UDP listener.")
